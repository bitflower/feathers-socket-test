
// Define TypeScript interface for service `atoms`. (Can be re-generated.)
// !code: imports // !end
// !code: init // !end

// tslint:disable-next-line:no-empty-interface
export interface AtomBase {
  // !code: interface // !end
}

// tslint:disable-next-line:no-empty-interface
export interface Atom extends AtomBase {
  // !code: more // !end
}

// !code: funcs // !end
// !code: end // !end
